<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>TITLE</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
   <!-- Required CSS Files [ BOOTSTRAP 4 ] -->
   <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css" integrity="sha384-WskhaSGFgHYWDcbwN70/dfYBj47jz9qbsMId/iRN3ewGhXQFZCSftd1LZCfmhktB" crossorigin="anonymous">
   <link rel="stylesheet" href="https://ajax.googleapis.com/ajax/libs/jqueryui/1.12.1/themes/smoothness/jquery-ui.css">

   <style media="screen">
      .nav a { padding: 0.6rem 2rem; }
      .body-bg { background-color: #999; }
      .card {
        display: flex;
        position: relative;
        display: -ms-flexbox;
        -ms-flex-direction: column;
        flex-direction: column;
        background-color: #f6f6f6;
        background-clip: border-box;
        border: 3px solid rgb(51, 48, 160);
        padding: 2rem 0rem 2rem 3rem !important;
        border-radius: 3.25rem;
      }
    </style>
   </style>
</head>

<body class="body-bg">
  <!-- Navbar -->
  <div class="top-row row">
      <nav class="nav navbar navbar-dark bg-dark justify-content-end col-md-12">
        <a class="text-light nav-link" href="/">Home</a>
      </nav>
  </div>

<!-- Main Container -->
    <div class="container">
      <div class="col-sm-10">
        <div class="row" style="margin-top: 10rem;">
            <div class="col-sm-8">
              <% if(typeof(errors) != 'undefined' ) { %>
                  <% for (var x in errors) { %>
                    <h3><%= errors[x].message %></h3>
                  <% } %>
              <% } %>
            </div>
        </div>

        <div class="card col-sm-10" style="padding:4rem 0rem 0rem 8rem;">
          <div class="row">
            <div class="row justify-content-center"><h1>Here are the Quotes</h1></div>
            <div class="col-sm-8">
              <% if(quotes.length >= 0) { %>
                <% for (var x in quotes) { %>
                  <h5><span class="text-muted"><%= "'" + quotes[x].text + "'" %></span></h5>
                  <h6><span class="text-danger"><%= quotes[x].name + "  -  " + quotes[x].createdAt %></span></h6>
                <% } %>
              <% } %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>
</html>